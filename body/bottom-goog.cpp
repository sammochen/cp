

void solve(ll goog) {
    cout << "Case #" << goog << ": ";
}

int main() {
    ios_base::sync_with_stdio(0), cin.tie(0);

    ll t = 1;
    cin >> t;
    rep(i, 1, t + 1) solve(i);
    return 0;
}
